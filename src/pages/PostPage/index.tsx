import { FC } from "react";
import Posts from "../../components/feature/Dashboard/Posts";

const PostsPage: FC = (): JSX.Element => {
  return (
    <div>
      <Posts />
    </div>
  );
};

export default PostsPage;
